{
  "name": "cc-helper-extension",
  "version": "2.0.0",
  "scripts": {
    "build": "esbuild popup.js --bundle --format=esm --outdir=dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4"
  },
  "devDependencies": {
    "esbuild": "^0.19.11"
  }
}